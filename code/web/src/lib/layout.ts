/**
 * Ancho de contenedor estándar para el contenido de una página privada.
 * Antes cada página competía con un ancho distinto (max-w-4xl/5xl/6xl);
 * este es el que ya usaba DashboardPage, adoptado como el único estándar.
 */
export const PAGE_CONTAINER =
  "container mx-auto max-w-[1400px] px-4 py-8 sm:px-6 lg:px-8";
